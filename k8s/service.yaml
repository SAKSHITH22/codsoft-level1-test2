apiVersion: v1
kind: Service
metadata:
  name: portfolio-website
  namespace: portfolio
  labels:
    app: portfolio-website
    environment: production
spec:
  type: ClusterIP
  selector:
    app: portfolio-website
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
